server:
  port: 8080

spring:
  datasource:
    name: all_scenic_spot
    url: jdbc:mysql://localhost:3306/all_scenic_spot?serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.test.bean